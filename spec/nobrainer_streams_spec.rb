require 'spec_helper'

describe NobrainerStreams do
  it 'has a version number' do
    expect(NobrainerStreams::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
